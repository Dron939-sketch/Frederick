# Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
asyncpg==0.29.0

# Cache
redis==5.0.1

# HTTP
aiohttp==3.9.1
httpx==0.25.1

# Utils
python-dotenv==1.0.0
pydantic==2.5.0

# Rate Limiting
slowapi==0.1.9

# Performance
uvloop==0.19.0
httptools==0.6.1

# Voice Services
edge-tts==6.1.9              # Microsoft Edge TTS
SpeechRecognition==3.10.4    # Распознавание речи
pydub==0.25.1                # Аудио конвертация
wave==0.0.2                  # Работа с WAV файлами

# Audio Processing (опционально, для лучшего STT)
# pyaudio==0.2.11            # Для записи звука (не нужен на сервере)
# ffmpeg-python==0.2.0       # Для конвертации аудио (нужен ffmpeg)
numpy>=1.24.0
webrtcvad>=2.0  # опционально, для более точного VAD
