# Core - без pydantic-core (он придет как бинарный пакет)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
asyncpg==0.29.0

# Cache
redis==5.0.1
aioredis==2.0.1

# HTTP
aiohttp==3.9.1
httpx==0.25.1

# Utils
python-dotenv==1.0.0
pydantic==2.5.0  # Не указываем версию pydantic-core

# Performance
uvloop==0.19.0
httptools==0.6.1

# Voice (опционально)
# librosa==0.10.1  # закомментировать если не нужно
# soundfile==0.12.1
